const request = require('super-test');
const server = require('../../api/server');

describe('Should be able to limit and control api data for users.', () => {

    const user = {
        sub: "github|26471655",
        role: "freeUser"
    }
})